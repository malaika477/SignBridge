# Slim dependencies for the web server only (Replit deployment).
# Desktop-mode packages (opencv-python, mediapipe, pyttsx3) are NOT needed
# by server/app.py and frequently fail to install on Linux hosts.
fastapi
uvicorn[standard]
numpy
pandas
scikit-learn
joblib
requests
python-dotenv